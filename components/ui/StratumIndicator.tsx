"use client";

/**
 * StratumIndicator — Phase 4 owns the real implementation.
 * Phase 1 ships an inert placeholder so the import surface is stable.
 */

export function StratumIndicator() {
  return null;
}
